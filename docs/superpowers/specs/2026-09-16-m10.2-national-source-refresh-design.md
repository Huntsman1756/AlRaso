# AlRaso M10.2 — National Source Coverage + Refresh (diseño)

**Estado:** PENDIENTE DE APROBACIÓN del owner.
**Fecha:** 2026-09-16
**Base de diseño:** `origin/main` = `97a60ee209ba4865f18d5d22c2731875b15092cc`
(merge PR #40 — EvidencePacket v2.1 consumer, evidence-only).
**Rama de trabajo:** `docs/m10.2-national-source-refresh` (worktree
`.worktrees/m10.2-design`).
**Clasificación:** ARQUITECTÓNICO + INFRAESTRUCTURA DE FUENTES. No es una feature
de producto, no publica reglas legales nuevas y no toca el resolver.

Esta spec fija la arquitectura de M10.2 antes de escribir una sola línea. M10.2
es la fase que convierte la pipeline de evidencia de M10.1 (5 pilotos
reproducibles) en infraestructura nacional mantenible: **atlas completo de
fuentes, refresh determinista y señales de versión**. M10.2 **no** publica
conclusiones jurídicas nuevas: la publicación de respuestas legales queda para
M10.3+ y siempre pasa por revisión humana.

## 0. Decisión resumida

Enfoque **C — híbrido** (aprobado por owner):

```text
M10.2-A  NATIONAL SOURCE ATLAS    caracterización completa, cero ingesta
M10.2-B  REFRESH CORE             primitiva genérica de snapshot/diff
M10.2-C  VERSION SIGNALS          candidatos mecánicos BOE/DOGC, nunca publicación
```

La separación se justifica así: **A** elimina la incertidumbre sobre las fuentes
(las gacetas no sondeadas no se pueden agrupar por familia sin caracterizarlas);
**B** implementa una primitiva genérica independiente del derecho material;
**C** consume esa infraestructura para señales de versión. La alternativa
"A — jurisdiction-first" queda descartada por evidencia (§A.2): el eje de
variación real es el mecanismo de la fuente, no la jurisdicción — Picos exige
tres canales de gaceta distintos para un solo parque.

**Frontera de entrega (decisión owner):** M10.2 es una arquitectura, **tres
ciclos de entrega independientes**:

```text
M10.2-A -> plan / rama / PR / gate / merge propios
M10.2-B -> plan nuevo sólo después de mergear la evidencia de A
M10.2-C -> plan nuevo sólo después de mergear B

NEXT WRITING-PLANS TARGET = M10.2-A ONLY
```

No se admite un único plan A+B+C: la propia descomposición demuestra tres
unidades independientes, y la frontera de entrega lo hace explícito.

## A. Base de evidencia

### A.1 Qué existe ya en `main` (medido, no asumido)

| Pieza | Estado en `97a60ee` |
|---|---|
| Discovery providers | `bocyl_opendatasoft`, `boa_cgi`, `boc_toc_post`, `dogc_socrata`, `boc_canarias_doc` + `discovery_request()` genérico (GET/POST, `doc_url_template`, `query_template`, `body_template`) |
| Fetch recipes | `get_simple`, `cgi`, `post_then_get`; transport inyectado; `SECLEVEL=1` para TLS legado (gencat) |
| Clasificación de ejecución | `reachability_observed` ∈ {REACHABLE, BLOCKED, UNREACHABLE, UNKNOWN} × `fetch_outcome` ∈ {SUCCESS, HTTP_ERROR, SOFT_404, CONTENT_MARKER_MISMATCH, TIMEOUT, TRANSPORT_ERROR}; `runner_network` ∈ {fixture, es_local, foreign_ci, unknown} |
| Parsers por formato | `bocyl_xml`, `boc_daily_xml`, `akn`, `boa_html`, `boc_canarias_html`, `pdf_text` (pdftotext opcional, `available=False` limpio), `html_marker` |
| VersionResolver | `gazette_publication` real + `consolidated_api` stub honesto; `effective_from` solo con evidencia estructurada + fragmento conservado; todo claim = `REQUIRES_MANUAL_REVIEW` |
| ReviewGate | `prepare_pr()` → ReviewPacket JSON + markdown: 9 eslabones, provenance, `last_verified`, `publication_readiness=NO` |
| Perfil declarativo | `source-profile.schema.json` validado por validador estructural stdlib; incluye `change_detection {method, cadence}` (declarado, aún no ejecutado) |
| Verificador | `tooling/m10_probe_verify.py`: OFFLINE_REPLAY (hard gate) + LIVE_VERIFY (gate de ejecución, reachability observacional) |
| EvidencePacket v2.1 | consumidor **evidence-only** mergeado (PR #40): `legal_rule_version` writes = 0, scope `REVIEW_REQUIRED`, sin efectos por keywords, sin fechas fabricadas |

### A.2 Evidencia que fija el diseño

1. **La heterogeneidad es de mecanismo, no de jurisdicción** (G0 §6, matriz
   M10.1): los 5 pilotos comparten código; toda diferencia vive en
   `profile`/`fixture`. No hay ningún `if jurisdiction` en `pipeline/`.
2. **El atlas G0 es parcial**: caracterizadas 9 superficies de gaceta (BOE +
   ES-AR/AN/AS/CB/CL/CN/MD/CT). Sin caracterizar: BOIB, DOCM, DOE, DOG, BOR,
   BORM, BON, BOPV, DOGV + boletines de Ceuta y Melilla.
3. **Refresh no existe**: `change_detection.method` está en el schema pero no
   hay snapshot store, comparador ni estado "último conocido". Es el gap
   estructural real de M10.2.
4. **`consolidated_api` es stub**: BOE `from/to` sobre `fecha_actualizacion` +
   `texto/indice` por bloque y DOGC `vigència` + `idVersion` AKN están
   *verificados en G0* (fuerza: fuerte) pero no implementados.
5. **No existe parser `boe_xml`** (consolidado por bloques) — pendiente desde
   la spec M10 §C.3.
6. **Geo-bloqueo probado** (P3 + `observatorio-alegaciones` en producción): un
   timeout extranjero no significa fuente ausente; el executor de refresh debe
   poder vivir en red española.

### A.3 OSS reuse gate — segunda pasada (2026-09-17)

Revisión real de repos, convención `ADOPT / ADAPT / COPY_PATTERN /
REFERENCE_ONLY / REJECT`:

| Repo | Licencia | Veredicto | Motivo |
|---|---|---|---|
| `legalize-dev/legalize-pipeline` | MIT | **COPY_PATTERN → ADAPT** | Maduró desde G0: `HttpClient` base (rate-limit/retry/backoff), `generic_daily` consolidado, parser `diario_boe` para normas **no consolidadas**, resolución de normas afectadas por reforma. Su `es/` sigue siendo solo-BOE — no resuelve gacetas autonómicas. ADAPT puntual del patrón de cliente BOE con cache/ETag en M10.2-C. |
| `OpenTermsArchive/engine` | (verificar en ADOPT) | **COPY_PATTERN** | Modelo exacto para M10.2-B: declaraciones JSON → repo de **snapshots** + repo de **versions** separados; **filtros de ruido anti-falso-positivo** (select/filter por documento); issues automáticos cuando el tracking falla (= `UNREACHABLE` observacional, nunca borrado). Se copia el flujo, no el código (Node.js). |
| `lowlydba/foul-flock` | MIT | **COPY_PATTERN** (confirmado) | Activo. Refresh automático que abre PR — no publica — con `last_verified` + reglas explícitas. |
| `ComputingVictor/MCP-BOE` | MIT | ADAPT (confirmado) | Patrones de cliente API BOE. |
| `opoalerta` | **AGPL-3.0** | **REFERENCE_ONLY** | Scrapers de los 17+2 boletines en producción con GitHub Actions diario — prueba de que todas las gacetas son accesibles. Copyleft → cero código. Su página `/estado` (última ejecución por fuente) es patrón de observabilidad. |
| `calendario-laboral-espana` | (verificar) | REFERENCE_ONLY | Matriz formato×CCAA ya sondeada (BOIB=HTML, DOG=RDF, Euskadi=JSON, DOGC=XML, resto mayoría PDF) — semilla de hipótesis para el atlas. |
| `videscar/dogv-ai` | (verificar) | REFERENCE_ONLY | Pipeline DOGV diario (sumario → issues → documentos → assets): evidencia de la estructura DOGV. |
| `indigo-akn` / `akn-profiler` | MIT | ADAPT (confirmado) | Referencia seria para Akoma Ntoso si DOGC exige más que el parser mínimo actual. |
| OWSLib | BSD-3-Clause | REFERENCE_ONLY | Solo si M10.2-A exige sondear servicios WFS nuevos; la geometría ya tiene provider propio. |

Veredicto del gate: **ningún OSS resuelve las gacetas autonómicas** (confirmado
por segunda vez); OTA y legalize aportan la forma del refresh y del cliente BOE.
No se justifica ninguna dependencia nueva: la pipeline sigue stdlib-only.

## B. Decisiones congeladas (no renegociables en M10.2)

Heredadas de M10/M10.1/PR #40, más las nuevas de esta fase:

```text
DISCOVERY != FETCH != PARSE != VERSION_RESOLUTION
GEOMETRY_DISCOVERY != LEGAL_SCOPE
AUTOMATED_EXTRACTION != PUBLICATION
VERIFIED_SOURCE != TRUE_INTERPRETATION
SOURCE_UNREACHABLE != SOURCE_ABSENT
PERMITTED != inferred from missing coverage

LEGAL_RULE_PUBLICATION = REVIEWED (humano, siempre)
EvidencePacket ingestion = evidence-only:
    legal_rule_version writes         = 0
    legal_review_complete auto-writes = 0
    spatial_review_complete auto-writes = 0

MONOLITHIC_SOURCE_ADAPTER = REJECTED
LEGAL_ENGINE (alraso/) = FROZEN — diff cero esperado
WEBAPP / qa/browser = FROZEN — diff cero esperado

VALID = effective_from/to | SYSTEM = recorded_at/until | publication_date
reachability = expectativa en perfil; verdad = campos de ejecución
```

Nuevas, propias de M10.2 (aprobación owner §P):

```text
ATLAS_SCOPE = 20 jurisdiction domains
  (17 CCAA + Ceuta + Melilla + Estado)
SOURCE_RECORD_COUNT = evidence-driven
  (un dominio puede necesitar varias superficies: buscador, API, diario,
   consolidado, ELI — discovery/fetch/versión pueden venir de superficies
   distintas; el gate es "20 dominios caracterizados", no "20 fuentes")

REFRESH_EXECUTION_REQUIREMENT = at least one ES-reachable executor
INITIAL_DEPLOYMENT            = es_local scheduled runner
foreign_ci                    = valid observer / degraded executor
ARCHITECTURE                  = runner-network agnostic
  (ningún provider depende semánticamente de "estar en España";
   reachability es observada, nunca propiedad eterna — hallazgo Cantabria)

CANONICAL EVIDENCE =
  {raw_sha256, canonical_sha256, canonicalizer_id, canonicalizer_version}
  same canonicalizer + same canonical hash   -> SAME / TRANSPORT_ONLY
  same canonicalizer + changed canonical hash -> CHANGED_CONTENT
  canonicalizer changed                       -> REBASELINE_REQUIRED
    (nunca clasificado automáticamente como cambio de contenido de fuente;
     el normalizador es parte de la provenance — cambiarlo no puede
     generar ni ocultar cambios)
  canonicalizers = conservative only: eliminan ruido demostrado
    empíricamente para esa familia de fuente; nada agresivo

DISAPPEARED_SUSPECTED = reachable-absence evidence ONLY
  (se alimenta exclusivamente de 404/410/soft-404 prerregistrado observado
   con el servidor respondiendo; UNREACHABLE —timeout, DNS, TLS, geo-block—
   nunca alimenta desaparición: SOURCE_UNREACHABLE != SOURCE_ABSENT.
   Thresholds por perfil: absence_threshold / invalid_threshold /
   degraded_threshold; contadores por superficie × runner_network —
   tres fallos de foreign_ci no contaminan el historial de es_local)

VersionClaim.RESOLVED =
  "la versión documental ha sido resuelta mecánicamente"
  NOT "la situación jurídica está resuelta"
  NOT "la regla es publicable"
  (la salida sigue siendo candidata; nunca crea legal_rule_version)
```

## C. Arquitectura por sub-milestone

### C.1 M10.2-A — NATIONAL SOURCE ATLAS

**Objetivo:** cada dominio jurisdiccional relevante para cobertura futura de
espacios protegidos tiene una ruta de fuente oficial caracterizada:

```text
por dominio / superficie:
  discovery        (mecanismo → doc IDs estables)
  fetch            (doc ID → bytes + formato + marcador)
  format/parser    (familia de formato existente o nueva)
  identifiers      (IDs nativos estables por tipo de documento)
  reachability     (expectativa por runner_network + fallback)
  change detection (mecanismo + fuerza + cadencia realista)
  version signal   (consolidado/vigència | ninguno verificado)
  fallback         (canal alternativo registrado)
```

**Ejecución:** probes reproducibles con la maquinaria ya existente —
`tooling/m10_probe_verify.py` generalizado por perfil — preservando por probe:
URL oficial, timestamp de retrieval, `runner_network`, HTTP outcome, content
marker, `raw_sha256`, IDs estables, licencia/términos de reutilización,
mecanismo de change-detection. Sin OCR salvo estructura que lo haga inevitable
y justificado explícitamente.

**Estado por dominio** (contrato del gate — un dominio no se "caracteriza" por
declaración):

```text
domain_status =
  PROVEN                  superficie(s) oficial(es) identificada(s) + probe
                          reproducible ejecutado con evidencia registrada
  | BLOCKED_WITH_EVIDENCE superficie oficial identificada + evidencia
                          reproducible de la limitación de acceso/
                          automatización (geo-block, sin superficie máquina,
                          WAF, etc.)
  | UNPROBED              trabajo no completado — NO satisface el gate

M10.2-A PASS exige: 20/20 dominios en {PROVEN, BLOCKED_WITH_EVIDENCE}
                    y 0 UNPROBED
```

`BLOCKED_WITH_EVIDENCE` es un resultado válido y honesto del atlas (no obliga a
inventar un scraper para Ceuta/Melilla); `UNPROBED` significa simplemente que el
trabajo no se ha hecho y no puede cerrar el milestone.

**Salidas (evidencia, no producto):**

```text
discovery/evidence/m10.2-atlas/            probes por dominio
pipeline/sources/atlas/*.profile.json      perfiles candidatos (con
                                           domain_status registrado)
source-atlas.json                          matriz completa
family clustering                          dominio → familia de mecanismo
reachability/fallback matrix               por dominio × runner_network
change-detection matrix                    mecanismo, fuerza, cadencia
```

**Hipótesis de clustering a verificar** (semilla G0 + OSS, no conclusión):

| Familia | Mecanismo | Cubierta ya por | Probables miembros nuevos |
|---|---|---|---|
| Open-data API | dataset query → doc IDs | `bocyl_opendatasoft`, `dogc_socrata` | Euskadi (JSON), DOG (RDF), Aragón CKAN |
| TOC/sumario → doc | tabla POST/GET → ID | `boc_toc_post` | DOGV, BON, BOIB? |
| URL estable por ID | `doc_url_template` + `get_simple` | `boc_canarias_doc` | BOPA, BOCM, BORM, DOCM, DOE, BOR |
| CGI legado | query string → text | `boa_cgi` | (probablemente único) |
| Consolidado por bloques | API de normas | — (stub) | BOE, DOGC ELI/AKN |

Si -A confirma esta hipótesis, el coste marginal de las ~11 superficies restantes
es **0–3 providers discovery nuevos** y **0–2 parsers nuevos** (RDF/DOG,
`boe_xml`). Lo que -A descubra manda sobre esta tabla.

**No ingesta de producción, no parseo a reglas, no PRs de normas.**

### C.2 M10.2-B — REFRESH CORE

**Objetivo:** detectar cambios de fuente/evidencia y producir deterministamente
un nuevo ReviewPacket/diff — o nada.

```text
snapshot store versionado (evidence versionada, no producto)
  SnapshotRecord:
    {source_id, surface_id, doc_id,
     raw_sha256, canonical_sha256,
     canonicalizer_id, canonicalizer_version,
     etag?, last_modified?,
     fetch_outcome, reachability_observed,
     observed_at, runner_network}
        |
        v  refresh run (por perfil, change_detection.cadence)
  clasificador determinista (contrato congelado):
        |
  FIRST_OBSERVATION
      -> BASELINE_CREATED -> snapshot persisted -> no ReviewPacket

  VALID FETCH + same canonicalizer:
      canonical same + raw same + transport metadata same
          -> SAME                    -> log; nunca ReviewPacket
      canonical same + (raw changed OR etag/last_modified changed)
          -> CHANGED_TRANSPORT_ONLY  -> log; nunca ReviewPacket
      canonical changed
          -> CHANGED_CONTENT         -> ReviewPacket via
                                        review.prepare_pr() existente

  canonicalizer id/version changed
      -> REBASELINE_REQUIRED         -> re-baseline manual; NUNCA clasificado
                                        como cambio de contenido de fuente

  timeout / DNS / TLS / network failure
      -> UNREACHABLE                 -> estado observacional registrado
                                        (NUNCA borrado, NUNCA "ausente")
      persistent UNREACHABLE (perfil: degraded_threshold,
                              por superficie × runner_network)
          -> ACCESS_DEGRADED         -> ReviewPacket de alerta
      UNREACHABLE NUNCA alimenta DISAPPEARED_SUSPECTED:
          SOURCE_UNREACHABLE != SOURCE_ABSENT

  reachable 404/410 o soft-404 prerregistrado, repetido según
  profile.absence_threshold (por superficie × runner_network)
      -> DISAPPEARED_SUSPECTED       -> ReviewPacket de alerta;
                                        la evidencia previa se conserva

  content-marker / parser contract failure
      -> INVALID
      persistente según profile.invalid_threshold observaciones
      consecutivas (por superficie × runner_network)
          -> ReviewPacket de alerta (fuente degradada)
```

**Reglas:**

- Los contadores de persistencia son **por superficie × `runner_network`**:
  fallos desde `foreign_ci` no contaminan el historial de un `es_local` que
  sigue accediendo correctamente. Los thresholds (`absence_threshold`,
  `invalid_threshold`, `degraded_threshold`) viven en el perfil de la fuente.
- El canonicalizer es stdlib, conservador y por familia: elimina solo ruido
  demostrado empíricamente (timestamps de fetch, tokens volátiles, contadores).
  `canonicalizer_id` + `canonicalizer_version` viajan en toda evidencia de
  comparación.
- Anti-falsos = doble hash + canonicalizer versionado, **en ambas direcciones**
  (patrón OTA endurecido — gate en §G):

  ```text
  CANONICALIZER_GATE
    known-noise fixtures:     raw cambia, canonical no debe cambiar
                              -> 0 falsos positivos
    semantic-mutation fixtures: cambios de texto significativos
                              -> canonical DEBE cambiar
                              -> 0 falsos negativos
    toda comparación conserva: raw_sha256, canonical_sha256,
                              canonicalizer_id/version, y la evidencia raw
                              o referencia raw reproducible
  ```

- La desaparición es una *sospecha revisable* construida solo con evidencia de
  ausencia alcanzable (404/410/soft-404 con servidor respondiendo), nunca con
  fallos de transporte; y nunca es un borrado.
- La apertura de PR sigue controlada por humano (patrón foul-flock: refresh
  automático → ReviewPacket → PR humano). Automatizar la apertura requiere
  aprobación separada.
- Bitemporalidad intacta: refresh solo escribe `recorded_at`/`recorded_until`
  en evidencia nueva; jamás muta `effective_*` ni reglas publicadas. Un fetch
  que contradice una regla publicada abre ReviewPacket con diff — nunca
  sobrescribe.
- Executor: deployment inicial `es_local` (scheduled runner); la arquitectura
  es agnóstica — `runner_network` se registra en cada observación y ningún
  provider asume red. `foreign_ci` es observador válido / ejecutor degradado.

### C.3 M10.2-C — VERSION SIGNALS

**Objetivo:** donde la fuente expone consolidación/vigencia mecánica,
producir `VersionClaim` candidatos reales en vez del stub.

```text
BOE    : list ?from&to sobre fecha_actualizacion (corpus)
         + texto/indice por bloque (change detection granular)
         -> parser boe_xml por bloques (familia nueva, una)
DOGC   : Socrata vigència + ELI + idVersion AKN
         (canal ya verificado en G0/P4)
```

**Contrato:**

- `RESOLVED` solo donde la resolución sea **mecánicamente demostrada** con la
  evidencia conservada en `resolver_evidence` (endpoint + respuesta + fecha).
- Todo lo demás sigue `REQUIRES_MANUAL_REVIEW` — es el default, no el fracaso.
- `effective_from` conservador (regla ya congelada en M10.1 §7.4): solo
  evidencia estructurada o fecha exacta prerregistrada; `vacatio` en texto
  libre → `null` + `REQUIRES_MANUAL_REVIEW`. Cero interpretación de vacatio.
- Candidato ≠ publicable: `RESOLVED` significa "versión documental resuelta
  mecánicamente"; no crea `legal_rule_version`, no toca `official_status`,
  no promueve scope.
- Consolidated/version APIs producen **VERSION CANDIDATES**: señales de
  evidencia, no verdad legal. `VERIFIED_SOURCE != TRUE_INTERPRETATION`.

## D. Respuestas a las preguntas del mandato

- **Qué es M10.2 / qué se difiere:** infraestructura de fuentes (atlas,
  refresh, señales de versión). Se difiere a M10.3+: publicación de nuevas
  respuestas legales, ingesta de producción al corpus, automatización de
  apertura de PRs.
- **Jurisdicciones cubiertas:** 20 dominios administrativos (17 CCAA + Ceuta
  + Melilla + Estado); número de fuentes = evidence-driven.
- **Providers M10.1 reutilizables sin cambio:** los 5 de discovery, las 3
  recipes de fetch, `discovery_request()`, el verificador, el ReviewGate —
  todos. Nuevos solo donde -A demuestre un mecanismo no cubierto.
- **Change detection:** `SnapshotRecord` + clasificador de la máquina de
  estados §C.2 sobre `CanonicalEvidence` versionada.
- **SAME/CHANGED/UNREACHABLE/INVALID:** §C.2 — máquina completa:
  `FIRST_OBSERVATION → BASELINE_CREATED`, `SAME`, `CHANGED_TRANSPORT_ONLY`,
  `CHANGED_CONTENT`, `UNREACHABLE`, `ACCESS_DEGRADED`, `DISAPPEARED_SUSPECTED`,
  `INVALID`, `REBASELINE_REQUIRED`.
- **ReviewPacket cuándo:** `CHANGED_CONTENT`, `ACCESS_DEGRADED`,
  `DISAPPEARED_SUSPECTED` e `INVALID` persistente. `SAME`,
  `CHANGED_TRANSPORT_ONLY`, `UNREACHABLE`/`INVALID` bajo threshold y
  `FIRST_OBSERVATION`: log, sin ruido.
- **Falsos positivos y negativos:** doble hash + canonicalizer conservador
  versionado; el `CANONICALIZER_GATE` exige 0 FP en fixtures de ruido y 0 FN
  en fixtures de mutación semántica; cambio de normalizador =
  `REBASELINE_REQUIRED`, jamás cambio de fuente.
- **Bitemporal:** refresh escribe solo `recorded_*` en evidencia nueva;
  reglas intocadas.
- **Desaparición/supersession:** `DISAPPEARED_SUSPECTED` solo desde ausencia
  alcanzable (404/410/soft-404) según `absence_threshold` del perfil;
  `UNREACHABLE` persistente → `ACCESS_DEGRADED`, nunca desaparición;
  evidencia siempre conservada; supersession como candidato de relación para
  revisión, nunca aplicada.
- **Reachability observacional:** `expected` en perfil vs `observed` en
  ejecución; divergencia = drift registrado.
- **Version candidates no publicables:** enum existente conservado;
  `RESOLVED` redefinido como resolución documental mecánica.
- **Fail-closed:** cada eslabón ausente produce estado explícito
  (`UNPROBED`, `UNREACHABLE`, `REQUIRES_MANUAL_REVIEW`,
  `REBASELINE_REQUIRED`) — nunca silencio, nunca `PERMITTED`.

## E. Impacto previsto en archivos (forecast, no implementación)

```text
pipeline/providers/version.py     consolidated_api real (BOE/DOGC)  [C]
pipeline/refresh/ (o providers/)  snapshot store + clasificador     [B]
pipeline/sources/atlas/           perfiles candidatos por dominio   [A]
pipeline/schemas/                 snapshot/atlas schemas            [A,B]
tooling/m10_*_verify.py           generalización atlas/refresh      [A,B]
discovery/evidence/m10.2-*/       probes atlas + refresh runs       [A,B,C]
tests/test_m10_*.py               contratos nuevos + stanza whitelist
docs/superpowers/specs|plans/     esta spec + plan posterior
```

`alraso/`, `webapp/`, `qa/browser/`: **diff cero esperado**. Si cualquier
sub-milestone exigiera tocarlos → STOP, señal de diseño erróneo.

## F. Riesgos y preguntas abiertas

1. **~11 superficies sin sondear** — la hipótesis de familias puede romperse;
   -A existe precisamente para medirlo antes de comprometer implementación.
2. **Canonicalizers por familia** — riesgo de normalización excesiva; mitigado
   por la regla "solo ruido demostrado" + `REBASELINE_REQUIRED`.
3. **Infra `es_local`** — el runner programado es decisión operativa;
   el diseño no la bloquea (agnóstico por `runner_network`).
4. **Licencias por fuente** — el atlas registra términos por fuente y
   artefacto; `DIGEST_ONLY` por defecto donde no estén verificados.
5. **Ceuta/Melilla** — boletines de ciudad con estructura propia; si no hay
   superficie automatizable verificable quedan `BLOCKED_WITH_EVIDENCE`
   (superficie identificada + limitación documentada) — resultado válido del
   atlas, no fracaso. `UNPROBED` nunca cierra el milestone.
6. **`boe_xml` por bloques** — formato nuevo; coste acotado a una familia,
   solo en -C.

## G. Gates de salida medibles

**M10.2-A:**
```text
20/20 dominios en {PROVEN, BLOCKED_WITH_EVIDENCE}; 0 UNPROBED
  PROVEN               = superficie oficial + probe reproducible ejecutado
  BLOCKED_WITH_EVIDENCE= superficie identificada + limitación documentada
family clustering + matrices commiteadas
0 ingesta de producción, 0 líneas en alraso//webapp//qa/browser/
```

**M10.2-B:**
```text
refresh sobre los 5 pilotos + perfiles atlas implementados:
  FIRST_OBSERVATION    -> BASELINE_CREATED, sin packet
  SAME                 -> sin packet (demostrado en fixtures)
  CHANGED_TRANSPORT_ONLY -> log, sin packet
  CHANGED_CONTENT      -> ReviewPacket determinista (mismo input = mismo packet)
  UNREACHABLE          -> estado registrado, evidencia intacta, sin packet;
                          persistente (degraded_threshold) -> ACCESS_DEGRADED;
                          NUNCA -> DISAPPEARED_SUSPECTED
  404/410/soft-404 alcanzable >= absence_threshold
                       -> DISAPPEARED_SUSPECTED + packet de alerta
  INVALID >= invalid_threshold -> packet de alerta (fuente degradada)
  canonicalizer bump   -> REBASELINE_REQUIRED, nunca CHANGED_CONTENT
  contadores por superficie x runner_network
CANONICALIZER_GATE: 0 falsos positivos (ruido conocido)
                    + 0 falsos negativos (mutación semántica)
apertura de PR: humana
```

**M10.2-C:**
```text
BOE fecha_actualizacion + DOGC vigència producen claims RESOLVED-candidato
  con provenance completa en documentos reales verificados
todo lo no demostrado mecánicamente -> REQUIRES_MANUAL_REVIEW
legal_rule_version writes = 0; review-complete auto-writes = 0
```

Global:
```text
NEW_RULES = 0
LEGAL_PUBLICATION = 0
PERMITTED inferido por ausencia = nunca
tests sin red; transport siempre inyectado; live solo en verificadores
```

## H. Self-review de la spec

- ¿Toda afirmación arquitectónica citada? Sí: cada decisión remite a G0,
  M10.1 (matriz de pilotos, código real en `97a60ee`) o al gate OSS de la
  segunda pasada; las hipótesis no probadas (clustering de familias) están
  marcadas como tales y son precisamente lo que -A mide.
- ¿Se cuela implementación? No: los shapes de §C son contratos de datos, no
  código; ningún archivo de `pipeline/` se propone modificar todavía.
- ¿Reduce garantías? No: las cuatro precisiones de la aprobación inicial y las
  cinco correcciones del SPEC_REVIEW_GATE están incorporadas (20 dominios ≠
  20 fuentes; runner agnóstico con `es_local` como deployment inicial;
  canonicalizer versionado + `REBASELINE_REQUIRED` + gate bidireccional;
  `DISAPPEARED_SUSPECTED` solo desde ausencia alcanzable con threshold por
  perfil y contadores por superficie × `runner_network`; `RESOLVED` =
  documental-mecánico; tres ciclos de entrega A→B→C).
- ¿Reproducible? La evidencia base está mergeada; los probes nuevos usarán el
  mismo verificador con transport inyectado y live gate de ejecución.
- ¿Límite legal? Reforzado: se fija explícitamente que `RESOLVED` y los
  version candidates no son publicables y que refresh jamás muta reglas.

## P. Registro de aprobación

Revisión owner sobre `def3d2a` (SPEC_REVIEW_GATE, 14 puntos): 0 P0, 4 P1,
1 P2 — enmienda aplicada en esta misma revisión:

1. **Máquina de estados refresh congelada** (§B, §C.2): `FIRST_OBSERVATION`,
   `SAME`/`CHANGED_TRANSPORT_ONLY`/`CHANGED_CONTENT` determinados por triple
   comparación, `UNREACHABLE` persistente → `ACCESS_DEGRADED` y **nunca**
   `DISAPPEARED_SUSPECTED` (solo ausencia alcanzable 404/410/soft-404 según
   `absence_threshold`), `INVALID` con `invalid_threshold`; contadores por
   superficie × `runner_network`.
2. **Canonicalizer gate bidireccional** (§C.2, §G): 0 falsos positivos en
   ruido conocido **y** 0 falsos negativos en mutación semántica; toda
   comparación conserva raw + canonical + canonicalizer id/version.
3. **`domain_status` del atlas** (§C.1, §G): `PROVEN |
   BLOCKED_WITH_EVIDENCE | UNPROBED`; el gate exige 20/20 en los dos
   primeros y 0 `UNPROBED`.
4. **Tres ciclos de entrega** (§0): A→B→C con plan/rama/PR/gate/merge
   propios; `writing-plans` solo para M10.2-A.
5. P2 documental: OWSLib MIT → BSD-3-Clause (§A.3).

Revisión owner sobre `aef5156` (SPEC_REVIEW_GATE final, diff de enmienda):
**0 P0, 0 P1, 0 P2 blocking → `M10.2_SPEC=APPROVED`**. Los cuatro P1
quedaron corregidos de forma material y los gates de salida reflejan las
mismas garantías. `NEXT=M10.2-A_WRITING_PLANS`; `M10.2-B` y `M10.2-C` no
autorizados; implementación no autorizada todavía.
