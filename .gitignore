__pycache__/
*.pyc
.pytest_cache/
*.db
dist/
build/
*.egg-info/
.venv*/
opencode.json

# LLM auxiliary tooling run log (safe metadata only, but never committed)
tooling/llm/runs.jsonl
*.env

# BDDAE/INSPIRE GML dump (63 MB, large binary — not committed)
docs/lineas_limite_gml.zip

# GML extraction temp directory (not committed)
tooling/_tmp_gml_extract/

# Builder build output directory (not committed)
tooling/m2b_build/

# Build output text (not committed)
tooling/build_output.txt
