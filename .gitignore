__pycache__/
*.pyc
.pytest_cache/
*.db
dist/
build/
*.egg-info/
.venv*/
opencode.json

# LLM auxiliary tooling run log (safe metadata only, but never committed)
tooling/llm/runs.jsonl
*.env

# BDDAE/INSPIRE GML dump (63 MB, large binary — not committed)
docs/lineas_limite_gml.zip

# GML extraction temp directory (not committed)
tooling/_tmp_gml_extract/

# Builder build output directory (not committed)
tooling/m2b_build/

# Build output text (not committed)
tooling/build_output.txt

# Gate comparison temp files (not committed)
tooling/_tmp_gisco_baseline_fixture.json
tooling/_tmp_gate_manifest.json
tooling/_tmp_boundary_gate_comparison.json

# Prepared DEM tile (auto-elevation); never commit the raster
webapp/data/dem/
# Orca kit: generated entries

.orca-local/

.orca-tools/

.opencode/node_modules/

# ── Sesión local / tooling personal (no es parte del repo público) ──
.opencode/
opencode.jsonc
deno.json
orca.toml
scripts/orca-local.ps1
uv.lock

# ── Trabajo local sin publicar (M2.2 preview deploy) ──
deploy/
docs/ALRASO-M2.2-VPS-PREVIEW.md
.env.example

# ── Research de parque nuevo congelado (M3a, local only) ──
discovery/evidence/m3a-*/
tooling/m3a_*
tooling/_*.py
